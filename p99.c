//WAP to entered 5 numbers and find the maximum number using arry.

#include<stdio.h>
int main()
{
    int a[5],i,max; 
    printf("Enter A Numebr:-");
    for(i=1;i<=5;i++)
    {
        scanf("%d",&a[i]);
    }
    if(max>=a[i])
    {
        printf("Maximum Number %d",max);
    }
        
}